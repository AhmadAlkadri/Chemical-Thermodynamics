# Steering Brief

## What changed since last brief (files + bullets)
- `src/chemthermo/eos/_pcsaft_density.py` (new), `src/chemthermo/eos/pcsaft.py`, `src/chemthermo/__init__.py` (slice `pcsaft-density-roots-flash`, ADR-0015)
  - **PC-SAFT is now usable for phase equilibrium.** `PCSAFTEOS` subclasses `chemthermo.models.EquationOfState` and implements `fugacity_coefficients(mixture=, temperature_K=, pressure_Pa=, composition=, phase=)`, so `stability_tp(..., eos=PCSAFTEOS())` and `flash_tp(..., eos=PCSAFTEOS())` work for non-associating mixtures. `components` became optional - `PCSAFTEOS()` takes the mixture's order; a given order that disagrees with the mixture raises rather than silently reordering.
  - **The new piece is a density root solver, and it lives in one private module.** At fixed `(T, x)` PC-SAFT is a function of the packing fraction `eta` alone, because all four `zeta` moments are proportional to the density. That single reduction buys three things at once: the whole scan grid is one vectorized call (so a root solve is affordable inside a flash loop), `Z = 1 + eta a'` and `(dP/drho)/RT = 1 + 2 eta a' + eta^2 a''` become *ordinary* derivatives (so `dP/drho` is analytic - it decides which roots are kept and is differenced through zero exactly where it matters), and the scan bounds are physical (`0 < eta < 0.7405`, hard-sphere close packing). Sign changes on a deterministic grid, safeguarded Newton, `dP/drho <= 0` discarded, bracket count reported so the rejected spinodal root is visible. No scipy.
  - **The seam is the existing fugacity interface, deliberately not a new phase-candidate type.** ADR-0014's "Next slice" note sketched PC-SAFT entering `stability_tp` as further `_PhaseCandidate`s. `_EOSTangentPlane` already holds two branches and keeps the lowest-Gibbs one, which is exactly the right rule for PC-SAFT's roots, so routing through `phase="vapor"/"liquid"` means **no solver changed at all** - `stability/`, `flash/` and `models/` are untouched, and every pinned PR / NRTL / modified-Raoult / VLLE number is unchanged by construction rather than by measurement. ADR-0015 decision 2 records what the candidate version would look like, what it would buy (a molar volume travelling with the phase into `FlashResult`) and why that is its own slice.
  - **Two recorded corrections to what the slice brief expected**, both physics rather than bugs: at `0.5 Psat` pure n-hexane at 300 K has **two** mechanically stable roots, not one (its liquid spinodal is at -40 MPa, so the stretched liquid exists at every positive pressure below saturation); and `z1 = 0.95` methane / n-hexane at 300 K / 1 MPa is **unstable**, not a stable vapour (the 1 MPa dew composition is `y1 = 0.9735`). Ledger Cases P-3 and P-4 state both.
  - **Cost accepted:** the hard-chain and dispersion terms now appear twice, once differentiated in `(rho, x)` and once in `eta`. The complexity receipt is in the module docstring and the cross-check is a test that pins the two derivations to 1e-12 relative.
- `tests/test_pcsaft_density.py` (new, 29), `tests/test_pcsaft_flash.py` (new, 19), `tests/validation/test_pcsaft_flash_vs_teqp.py` (new, 14), `tests/test_pcsaft.py`
  - The decisive validation needs no reference tie line: **teqp's own `get_fugacity_coefficients`, evaluated at chemthermo's converged compositions and densities, must make the two phases equal in fugacity** (worst 3.8e-9 relative over seven pressures). Alongside it, teqp traces and polishes its own 300 K isotherm - tie lines to |dx1| <= 2.0e-9, |dy1| <= 3.7e-12, densities to 1.3e-9 relative - and bubble pressures found by bisecting `stability_tp`'s *verdict* (chemthermo solves no bubble-point equation) match teqp's `mix_VLE_Tx` to 1.4e-8 relative. `tests/test_pcsaft.py` changed one assertion: `PCSAFTEOS(components=())` no longer raises at construction, the failure moved to the point of use.
- `examples/basic/flash_tp_pcsaft_demo.py` (new), `examples/validation/14_pcsaft_flash_vs_teqp.py` (new), `README.md`, `examples/README.md`, `.agents/brain/adr/0015-pcsaft-density-roots-flash.md` (new), `.agents/brain/brain.md`, `.agents/brain/validation-cases.md`
  - README's "Limits, stated plainly" rewritten: the "no density solver" and "not wired into flash" gaps are discharged and replaced by the ones that remain (two phases at most on phi-phi, `kij` is the user's to justify, no temperature derivative, no phase densities in `FlashResult`, unresolved root pairs closer than the grid step). Ledger Cases P-3, P-4, P-5 added.
- `src/chemthermo/eos/pcsaft.py`, `src/chemthermo/parameters/pcsaft.py`, `src/chemthermo/parameters/data/eos/pcsaft.json` (new), `src/chemthermo/models/_kij.py` (new), `src/chemthermo/models/peng_robinson.py`, `src/chemthermo/__init__.py`, `src/chemthermo/parameters/__init__.py` (slice `pcsaft-residual-helmholtz`, ADR-0014)
  - **PC-SAFT is no longer a placeholder.** `PCSAFTEOS.residual_helmholtz` used to look up parameters that did not exist and then raise `NotImplementedError`; `tests/test_pcsaft_sanity.py` asserted the status string `"missing_parameters"`. It now implements the non-associating Gross & Sadowski (2001) model - hard chain plus dispersion - behind the **unchanged** `EOSProtocol` signature, and gains `compressibility_factor`, `pressure_Pa` and `ln_fugacity_coefficients`, all taking `(temperature_K=, density_mol_m3=, composition=)`.
  - **The state is `(T, rho, x)` on purpose.** PC-SAFT is Helmholtz-explicit, so a `(T, P)` state is *several* states until a density root is chosen, and this slice does not choose: no root solver, no `EquationOfState` subclassing, no `flash_tp`/`stability_tp` wiring. Shipping a half-finished root finder to fill the pressure-based signature would have been the same scaffolding this slice exists to remove. Between the spinodals `Z < 0`, `pressure_Pa` returns the real negative pressure, and `ln_fugacity_coefficients` raises `ModelError` instead of handing back a `nan`.
  - **Derivatives are analytic, but assembled rather than transcribed.** Two structural facts replace ten appendix equations: every `zeta_n` is proportional to the density (so `rho d/drho` of anything built from the moments is `sum_n zeta_n d/d zeta_n`), and `a_disp` depends on composition only through `eta`, `mbar`, `m2es3`, `m2e2s3`. `Z` and `partial a_res/partial x_k` then follow from the gradient of `a_hs`, the gradient of `g_ii`, and four partials of one scalar. **This caught a real bug**: `partial a_disp / partial m2e2s3` was written `-pi mbar C1` instead of `-pi mbar C1 I2`. It is invisible in `A^res` and `Z`, invisible for *every* pure component (the term cancels), and shows up only in mixture `ln phi` - 0.060 out on a methane/n-hexane binary, with `sum_i x_i (d ln phi_i)` still exactly zero, so the Euler identity alone would not have found it either. teqp did.
  - **`C1` uses the typo-corrected Eq. (A.11).** The paper prints `C1 = (...)^-1 = (...)`, dropping the outer exponent on the right-hand side; NIST TRC's PC-SAFT page states the erratum. The reciprocal is implemented and the test pins the *shape* (`C1 * bracket == 1`, and `0 < C1 < 1` at `eta > 0`), not just a number, because the two readings coincide at `eta = 0`.
  - **The `kij` contract is shared, not copied.** `KijInput`, `KijPairs`, `canonicalize_kij` and `kij_matrix` moved from `peng_robinson.py` to the internal `models/_kij.py` with their bodies unchanged (the only edit is that the error-message prefix is now an argument, so PR's strings are byte-identical). Peng-Robinson stays bit-identical; `tests/test_pr_eos.py` and `tests/test_flash_refactor_bit_identity.py` are untouched and pass.
  - **Parameters got the NRTL treatment.** `PCSAFTParameters` loads a packaged `data/eos/pcsaft.json` (schema_version 1, `provenance` block, per-component `m` / `sigma_A` / `epsilon_k_K` / optional `MW_g_mol` / `source`) with the 11 non-associating compounds of Table 1 whose names also exist in the databank, plus `from_records(...)` and `for_components(names)`. The stub `PCSAFTParameterRegistry` is **removed** (never in `chemthermo.__all__`, only ever raised); `PCSAFTParameterError` is unchanged and still exported.
- `tests/test_pcsaft.py` (new, 59), `tests/validation/test_pcsaft_vs_teqp.py` (new, 18), `tests/test_pcsaft_sanity.py` (deleted)
  - **Case P-1, teqp cross-check: worst `|dA^res/RT| = 4.4e-15`, worst `|dZ| = 2.6e-14`, worst `max_i |d ln phi_i| = 2.7e-14`** over 14 states, against an asserted 1e-10 - four orders of margin, at the level of floating-point summation order. States cover pure/binary/ternary, gas-like and liquid-like densities (`eta` up to 0.45), `k_ij = 0.03`, and one inside the spinodal where the refusal is asserted. teqp takes every derivative by autodiff, so this tests the hand-written derivatives, not only the model.
  - **Case P-2, saturation:** a bisection density root finder written *in the test* reproduces teqp's `pure_VLE_T` for n-hexane to `2.9e-12` (Psat), `1.2e-16` (rho_L) and `4.2e-12` (rho_V) relative at 300 K, and `3.0e-13 / 1.4e-15 / 1.0e-13` at 400 K, against an asserted 1e-6.
  - **Case P-0, internal route:** independently written pure-component formulas (Carnahan-Starling `a_hs`, `g = (1 - eta/2)/(1 - eta)^3`) agree to 1e-13; `Z` matches the density finite difference to 1e-8; `mu_i^res` matches the mole-number finite difference of `n A^res` at fixed `(T, V)` to 1e-7; the Euler identity holds to `8.9e-16`.
  - **The Gibbs-Duhem test had to be derived, not copied.** `sum_i x_i d ln phi_i = 0` holds at fixed T **and P**. These `ln phi` are at fixed T and *density*, where the pressure moves with composition, so the correct statement - obtained by differentiating the Euler relation at fixed `(T, V, n_total)` - is `sum_i x_i d ln phi_i = (1 - 1/Z) dZ`. That is what the test asserts; writing the textbook form would have been a test that fails on correct code.
  - **A negative control guards the whole comparison:** perturbing one `sigma` by 1 % moves `A^res/RT` by 4.2e-03, so the 1e-10 agreements cannot be passing because the reference ignores its inputs.
- `examples/basic/pcsaft_properties_demo.py` (new), `examples/validation/13_pcsaft_vs_teqp.py` (new), `README.md`, `examples/README.md`, `pyproject.toml`, `tools/smoke_install.py`, `.agents/brain/adr/0014-pcsaft-residual-helmholtz.md` (new), `.agents/brain/brain.md`, `.agents/brain/validation-cases.md`
  - Golden paths for the properties and for the whole PASS/FAIL cross-check (the latter prints a message and exits 0 without teqp). README gained a "PC-SAFT (`chemthermo.eos`)" section with a "Limits, stated plainly" subsection naming all four gaps (non-associating only, no density solver, not wired into flash, no temperature derivative). `teqp>=0.23` joins the `validation` extra; the packaged JSON joins `package-data` and the install smoke check now asserts that it ships **and** that a PC-SAFT number comes out right from a clean non-editable install.
  - **Parameter provenance is recorded honestly.** The primary source (Gross & Sadowski 2001) is paywalled and **was not read**: ACS returns HTTP 403 from this environment. Both tables were transcribed from two independent secondary sources each, checked entry by entry with zero differences - universal constants from teqp's `src/data/PCSAFT.cpp` and Wikipedia's PC-SAFT article; the 11 parameter triples from FeOs' `gross2001.json` and Clapeyron.jl's `PCSAFT_like.csv` (whose `source` column is this paper's DOI), with teqp's built-in library independently confirming methane/ethane/propane. The citation stays the paper; the fact that it was not opened is written into the JSON, the ledger and the test docstrings.
- `src/chemthermo/vlle/__init__.py`, `src/chemthermo/vlle/loader.py`, `src/chemthermo/flash/tp.py` (slice `vlle-plugin-deprecation`, ADR-0013)
  - **`chemthermo.vlle` is now deprecated, not "deliberately untouched."** ADR-0011 deferred the disposition of this public plugin boundary (`get_vlle_engine`, `VLLEEngine`, `VLLEResult`) to a follow-up ADR; ADR-0013 answers it: deprecate, do not repurpose or delete yet. Importing `chemthermo.vlle`, or calling `get_vlle_engine()`, now emits `warnings.warn(..., DeprecationWarning, stacklevel=2)` naming the in-tree replacement (`flash_tp(..., flash_mode="modified-raoult")` with `FlashSettings(max_phases=3)`, ADR-0011); the exported names and the loader's `VLLEPluginNotInstalledError`/`VLLEPluginError` behavior are otherwise unchanged for one deprecation cycle.
  - **`flash_mode="vlle"` still raises `ModelError`, with a different message.** It no longer tells the caller to install `chemthermo_vlle` - the plugin boundary was found not to answer their question even when installed (see below) - it now says three-phase equilibrium is discovered automatically by `"modified-raoult"` and points to ADR-0011 and ADR-0013.
  - **Why not repurpose the boundary as the way to reach a different engine (a user's own solver, PC-SAFT-backed VLLE)?** `VLLEEngine.solve()`'s shape (one opaque call, a flat result, no stability/candidate/phase-set diagnostics) cannot carry what the in-tree path now reports without becoming a second, driftable definition of the same information, and nothing in or out of this repository currently implements the protocol. A future PC-SAFT-backed engine enters the way ADR-0011 already said future models should: as further `_PhaseCandidate`s behind the existing `EquationOfState`/`stability_tp`/`flash_tp` contracts, not a new "solve everything" protocol.
  - **The private sibling `chemthermo_vlle` disposition is recorded, not acted on.** Read read-only from this orchestrator's seat (`~/Documents/GitHub/chemthermo_vlle`, not part of this repository or its CI): a 164-line scaffold whose `solve_vlle()` unconditionally returns `VLLEOutput(status="not_implemented", phases=None)`, containing no phase-equilibrium physics, pinned (`pyproject.toml`) to chemthermo commit `eb9cfda7`, a January 2026 SHA that predates every stability/flash ADR from 0005 onward. ADR-0013 recommends the maintainer archive or deprecate it rather than develop it further, and states what a future private engine could still legitimately differentiate on: optimized numerical kernels, non-public parameter packs, initialization/continuation strategies for hard multi-phase regions, and batch/grid solve infrastructure - all downstream of the public contracts, none requiring a parallel `VLLEEngine`-shaped protocol.
  - ADR-0001 amended: its listing of `chemthermo.vlle` as a "documented public subpackage" now says deprecated, pointing at ADR-0013.
- `tests/test_vlle_plugin.py` (+1 test), `tests/test_flash_api.py` (+1 test)
  - `test_importing_vlle_warns_deprecation` re-imports the module (`importlib.reload`) inside `pytest.warns(DeprecationWarning)` to catch the module-level warning; `test_vlle_plugin_missing_raises` now wraps the existing `VLLEPluginNotInstalledError` assertion in `pytest.warns(DeprecationWarning)` too, since `get_vlle_engine()` warns on every call, independent of import time. A new `test_flash_tp_vlle_mode_points_to_modified_raoult` pins the new `flash_mode="vlle"` message text and asserts (via `recwarn`) that this call path does **not** emit `chemthermo.vlle`'s warning - the two deprecations are independent triggers, and a bare `ModelError` must not silently import the deprecated subpackage.
- `README.md`, `.agents/brain/adr/0013-vlle-plugin-deprecation.md` (new), `.agents/brain/adr/0001-public-api-truth-source.md`, `.agents/brain/brain.md`
  - README's "chemthermo.vlle is untouched" bullet replaced with a "Multiphase equilibrium is in-tree" pointer stating the deprecation and that nothing is removed yet. `brain.md`'s public-API and roadmap sections updated to match (ADR-0013 in the index, `chemthermo.vlle` marked deprecated, the old `vlle-plugin-boundary-disposition` slice replaced by this completed one, and `pcsaft-residual-helmholtz` (Stage F) promoted to the next roadmap slice - it was already the only other item queued there).
- `src/chemthermo/stability/_evaluator.py`, `src/chemthermo/stability/tp.py`, `src/chemthermo/stability/results.py` (slice `stability-candidate-surfaces`, ADR-0012)
  - **A tangent-plane trial now runs on one fixed phase-candidate surface.** An initial estimate may name the candidate it belongs to (`_InitialEstimate(label, w0, surface)`); the solver then evaluates *that* candidate at every SSI and Newton iterate. An estimate that names no surface calls `ln_fugacity_terms` exactly as before, which is how the bit-identity guard is met by construction rather than by testing afterwards.
  - **Why the asymmetry is the decision, not an omission.** A cubic's missing compressibility root is the *same* model failing to exist at that composition, so minimum-Gibbs root selection at every iterate stays right there (ADR-0005, Cases S-4/F-1). The modified-Raoult pair is an activity liquid and an ideal gas - two *different* models, both smooth everywhere - so re-selecting between them makes the fixed-point map discontinuous across the surface where they cross. That is a different, non-monotone iteration, and it was hiding a real instability.
  - **The measured defect.** At 363 K, the Case V-2 feed z = (0.15493061, 0.04905728, 0.79601210): `tpd` is +8.07e-04 at x^I, +1.33e-03 at x^II and **-9.92e-03** at the equilibrium vapor, yet every trial collapsed to the trivial solution and `stability_tp` said **stable**. From the Raoult-vapor start the liquid is the lower-Gibbs candidate at the intermediate compositions, so the update used the liquid terms. Reproduced as a test *before* the fix, and still reproducible by stripping the surface labels (`test_without_a_fixed_surface_the_same_feed_is_reported_stable`).
  - **The trial count did not grow.** The ideal-gas term is identically zero, so the vapor-surface map is the constant map `ln W_i = d_i`: one substitution reaches the surface's unique stationary point from *any* start, with an exactly zero residual next. Pure-component starts on the vapor surface would all return the same point, so they are not run and the reason is written down. Final set: `raoult-vapor` (vapor), `raoult-liquid` (liquid), `pure-<name>` per component (liquid).
  - **`tpd` is still the lowest-Gibbs value at the converged point** - the distance is to the lower envelope, not to one sheet - while the stationarity residual is measured on the trial's own surface. `StabilityTrial` records both (`surface`, `phase_branch`) plus `surface_fallback`; diagnostics gain `trial_surfaces`, `surface_fallback_trial_count`, `minimizing_trial_surface`, all absent when no trial named a surface so the EOS/activity diagnostics mappings are unchanged.
- `tests/validation/test_vlle_verdict_map.py` (new, 18), `examples/validation/12_vlle_verdict_map.py` (new), `tests/test_stability_candidates.py` (+9), `tests/test_flash_vlle.py` (+1), `tests/validation/test_vlle_water_propanol_butanol.py`
  - **Validation Case V-5: the verdict map.** 76 / 75 / 76 feeds at 363 / 364 / 365 K, each classified independently by building *every* admissible state (tie-triangle, a four-equation VL Newton, a seven-equation LL Newton, the single phase) and taking the lowest Gibbs energy - so "inside the triangle means three phases" is a result, not an input. **Zero disagreements** at all three temperatures: (1,1)/(2,2)/(3,3) = 48/7/21, 44/10/21, 41/14/21 with every off-diagonal entry 0. Three-phase compositions equal the independent triangle to **9.5e-12** and fractions to **1.6e-11**; two-phase equilibrium residual **<= 3.8e-13**, mass balance **<= 1.1e-16**.
  - **The classifier had to be widened twice, and the flash was right both times.** Two feeds (363 K z = (0.0833, 0.1667, 0.75), LL; 365 K z = (0.1667, 0.0833, 0.75), VL) were missed by a single-seed independent solver and would have been recorded as disagreements. Multi-start Newton found both, agreeing with `flash_tp` and with a lower `G/RT` than the single phase. Recorded in Case V-5 so the next reader knows the reference needed the work, not the code under test.
  - **Case V-2's pinned miss is retired**, replaced by the three-phase assertion (compositions and fractions to 1e-8 against the solved triangle). **What remains there is narrower and recorded, not accommodated:** the `pure-Water` liquid-surface trial still stalls, because the stationarity Jacobian at w = (0.13762, 0.04163, 0.82075) has eigenvalues {**3.26e-09**, 1.106, 1.000} (cond **8.2e+08**) - a plait-point property, not arithmetic; nothing is non-finite and the `nan` tpd is the documented sentinel for an unconverged trial. SSI on that surface does converge to the trivial solution, in ~1.2e+04 iterations against `max_iter = 300`. The verdict does not depend on it.
- `.agents/brain/adr/0012-stability-trial-candidate-surfaces.md` (new), `README.md`, `.agents/brain/brain.md`, `.agents/brain/validation-cases.md`, `examples/README.md`
  - ADR-0012 records the asymmetry between cubic roots and heterogeneous candidates as the load-bearing decision; README's multiphase section gains the fixed-surface paragraph with its limits; the brain's "stability trial set near a plait point" risk is struck through and replaced with the narrower one above.

- `src/chemthermo/flash/_multiphase_rr.py` (new), `src/chemthermo/flash/_multiphase.py` (new), `src/chemthermo/flash/_verify.py`, `src/chemthermo/flash/_assemble.py`, `src/chemthermo/flash/_detect.py`, `src/chemthermo/flash/settings.py`, `src/chemthermo/flash/tp.py` (slice `flash-vlle-phase-addition`, ADR-0011)
  - **The phase count is now discovered up to `FlashSettings.max_phases` (new, default 3)** on the `modified-raoult` path: after any converged phase set fails its post-split stability test the incipient phase found there is added and the set re-solved; a phase whose fraction converges to zero or below is removed. `diagnostics["phase_set_history"]` records the route, e.g. `"L -> LV -> LLV"` or `"V -> LV -> LLV -> LL"`.
  - **Multiphase Rachford-Rice as a convex minimization.** The `NP-1` RR equations have a symmetric Jacobian, so they are the gradient of `F(beta) = -sum_i z_i ln t_i` (Okuno, Johns & Sepehrnoori, SPE J 15 (2010) 313), whose Hessian `A^T diag(z/t^2) A` is PSD: one minimum or none, never a spurious root. Newton inside Okuno's region `a_i . beta <= b_i`, `b_i = min(1-z_i, min_j(1-K_i^j z_i))`, derived from `x_i^j >= 0` alone - it contains **no pole**, unlike the Leibovici-Neoschil `t_i >= 0` whose boundary *is* the pole set. Initial estimate = mean of the vertices of `S` cap `{beta_j >= 0, sum beta_j <= 1}`, falling back to the vertices of `S` for a negative flash.
  - **Two deviations from the paper, both measured and recorded.** (1) The stopping test had to be made **scale free** - `max_j |f_j|` divided by `sum_i z_i |K_i^j - 1| / t_i`, floored at 1 - because `f_j` is a cancelling sum of terms of that size and one phase set holds a vapor/liquid `K` of 30 next to a liquid/liquid `K` of 1.1; with an absolute 1e-12 the n-butanol/water liquid pair stalls at 2.0e-11 and the whole flash fails. (2) The line search accepts an Armijo decrease of `F` **or** a decrease of the residual, because near the minimum the change in `F` drops below the float resolution of `F` itself. Same rule the ADR-0009 stage already used.
  - **Negative flash *is* the removal mechanism.** `S` constrains compositions, not phase amounts, so `beta_j <= 0` is a meaningful converged answer meaning "this phase is not here". No positive threshold is used: Okuno Example 3 converges to a genuine `beta = 2.2e-06`. When the region *recedes* (no minimum at all - exactly what Gibbs' rule says about three phases in a binary away from T3) the solver reports the recession and names the phase whose amount runs to minus infinity; that is the one removed.
  - **Multiphase second-order stage**: the ADR-0009 Gibbs minimization generalized from 1 to `NP-1` non-reference phases; gradient still exactly the equal-fugacity residual by the same Gibbs-Duhem cancellation. Needed: SSI alone takes **324 iterations** to reach 1e-12 on the validated tie-triangle, where the Newton stage reaches **7e-16 in 3** from a 20-iteration start.
  - **`_post_split_report` replaces the raise-only `_post_split_stability`** for the path that can add a phase, returning the instabilities and their minimizers; the old function keeps the old message for phi-phi and gamma-gamma. The partner-is-marginal rule now reads *every* other phase (identical for two).
  - **Bit-identity was the binding constraint on the diagnostics contract.** The new keys (`phase_set_history`, `phases_added`, `phases_removed`, `delta_g_vs_two_phase_rt`, `rachford_rice_iterations`) appear **only** on results that actually entered the search, because `tests/test_flash_refactor_bit_identity.py` compares whole `diagnostics` mappings with `==` and any added key on an existing path would break it.
- `tests/test_multiphase_rr.py` (new, 16), `tests/test_flash_vlle.py` (new, 11), `tests/validation/test_vlle_water_propanol_butanol.py` (new, 21)
  - Okuno Table 1: Example 3 reproduces the published `(0.87, 2.2e-6)` as **(0.8701633569, 2.180303e-06)** in 4 Newton iterations; Example 4's negative flash returns **(1.2, 14.66, -14.86)**, the exact linear solve of `z = sum_j beta_j x^j` from the paper's own printed compositions. Convexity checked (PD Hessian + no feasible descent), not assumed. Iteration counts differ from the paper (8 and 7 vs 4 and 4 for Examples 1-2) - different initial estimate, line search and tolerance; recorded, not matched.
  - Ternary tie-triangle at 363/364/365 K from an independent six-equation Newton solve (residual <= 6.7e-16) whose *unsolved* equations (liquid II's bubble point, the shared vapor) are checked as consequences. Six interior feeds: |dx| <= **2.2e-14**, |dbeta| <= **1.6e-13**, equilibrium residual <= **1.8e-15**, mass balance <= **6.9e-18**, post-split `tpd_min` >= **-4.1e-16**, and **G3 < G2 < G1** computed in the test (364 K centroid: -0.693912758 < -0.693543323 < -0.691218474). The orchestrator's reference feeds reproduce to the last printed digit.
  - The 0.135 K refusal window of Case R-3 now returns the binodal tie-line (**0.019998419467 / 0.359999661508**, deviation 3.3e-13) via `"V -> LV -> LLV -> LL"`, agreeing with a direct `gamma-gamma` flash to 1.8e-12 and with the lever rule exactly. `max_phases=2` still raises the documented error.
  - **Finding, recorded not accommodated.** At 363 K the two liquid vertices differ by only 0.053 in `x_1`; for a feed weighted towards them (inside the triangle) every stability trial collapses to the trivial solution, `tpd_min = 0.0`, and `flash_tp` returns a single liquid - **wrong for this model**. The failure is in the *stability* trial set, not in the search, which is never entered. Pinned by test so an improvement is noticed. No trial was added to chase it.
  - **No external reference exists - checked, not assumed.** `FlashVLN` with **two distinct** `GibbsExcessLiquid` objects still reports `unique_liquid_count == 1` and raises `TypeError: 'NoneType' object is not subscriptable`; with one it returns a **two-phase** answer at a feed inside the tie-triangle, whose liquid (0.09928882, 0.07716435, 0.82354683) is neither conjugate liquid. Adjudicated by Gibbs energy computed in the test: ours **-0.693912758** vs theirs **-0.693543321**. The *intermediate* is externally confirmed though - `thermo`'s two-phase answer matches chemthermo's own two-phase candidate to **2.4e-09** in `G/RT`, so the disagreement is about the phase count, not the two-phase thermodynamics. The failure is asserted so a future `thermo` fix is noticed.
- `examples/basic/flash_tp_vlle_demo.py` (new), `examples/validation/11_vlle_water_propanol_butanol.py` (new), `examples/validation/10_modified_raoult_water_butanol.py`, `tests/test_flash_modified_raoult.py`, `README.md`, `examples/README.md`, `.agents/brain/adr/0011-flash-phase-addition-removal.md` (new), `.agents/brain/validation-cases.md`, `.agents/brain/brain.md`
  - Golden paths for the tie-triangle and for the whole PASS/FAIL validation (triangle + four negative controls + the refusal window); README section "Three phases, and how many there are" with the limits stated (the cap does not choose the answer, only `modified-raoult` searches, the diagnostics keys are conditional, the phase count is never better than the stability test, no performance work); Case R-3's script and test rewritten so the old refusal is pinned under `max_phases=2` and the resolved answer under the default; validation Cases V-1..V-4.
  - **`chemthermo.vlle` deliberately untouched (at the time).** The public plugin boundary and `flash_mode="vlle"`'s `ModelError` still pointed at an out-of-tree engine for a capability that was now in-tree. ADR-0011 recorded that a follow-up ADR, `vlle-plugin-boundary-disposition`, would decide whether that boundary is repurposed or retired; changing a public API is its own decision. **Answered by ADR-0013 (see the top of this brief): deprecated, not repurposed.**

- `src/chemthermo/models/_antoine.py` (new), `src/chemthermo/stability/_evaluator.py`, `src/chemthermo/stability/tp.py`, `src/chemthermo/flash/_detect.py`, `src/chemthermo/flash/_split.py`, `src/chemthermo/flash/_second_order.py`, `src/chemthermo/flash/_verify.py`, `src/chemthermo/flash/tp.py`, `src/chemthermo/flash/settings.py` (slice `flash-modified-raoult`, ADR-0010)
  - **Phase candidates.** The internal tangent-plane evaluator now holds a *set* of `_PhaseCandidate`s and `_select_min_gibbs` keeps the one minimizing `sum_i w_i term_i(w)` - the only candidate-dependent part of `G/RT`. That is one rule for three families: the cubic's two compressibility roots, a single activity liquid (label `None`, because nothing was selected), and the new modified-Raoult pair. The EOS and activity-only adapters are the two-candidate and one-candidate special cases, with the same float operations, the same order and the same error strings; the flash bit-identity fixture and the pinned Peng-Robinson stability numbers are untouched. A candidate's `optional` flag separates "this root may not exist here" (skipped) from "this model failed" (re-raised) - the second case must not be answered with the *other* candidate.
  - **`stability_tp(..., activity_model=..., vapor="ideal")`**: adds an ideal-gas candidate to the activity liquid, whose term becomes `ln gamma_i + ln(Psat_i/P)` with `Psat` from the databank Antoine records. `feed_branch`/`phase_branch` then say `"liquid"`/`"vapor"` - i.e. what the feed *is* and what the incipient phase *is*. `vapor="ideal"` with an `eos` is a `ModelError`; full gamma-phi against an EOS vapor is still refused (ADR-0007 is narrowed, not overturned).
  - **`flash_tp(..., flash_mode="modified-raoult")`**: the candidate labels decide the regime, so VLE returns `liquid`/`vapor` with a real `vapor_fraction` and LLE returns `liquid1`/`liquid2` with `None` - no volatility-ordering heuristic is needed here, unlike phi-phi. The split gives each phase its assigned candidate and the K update collapses to `K_i = exp(t_i^x(x) - t_i^y(y))`, which *is* `gamma_i Psat_i / P` for a liquid/vapor pair and `gamma_i^I/gamma_i^II` for two liquids. The ADR-0009 second-order stage was generalized to two term functions (gamma-gamma passes the same one twice, so nothing moved there). Post-split re-tests **both** phases against **both** candidates. The mode is deliberately **never inferred**: an activity model alone still means `gamma-gamma`.
  - **Antoine is enforced, not extrapolated.** `ln(P^sat/bar) = A - B/(T+C)` (Koretsky 2012; water gives 1.0131 bar at 373.15 K). Outside `[Tmin_K, Tmax_K]` the call raises `InputRangeError`; the mixture's window is reported in diagnostics.
  - `flash_mode="gamma-phi"` marked **DEPRECATED** in docstrings and README, with the physical reason (it multiplies `gamma` by an EOS liquid `phi`, double-counting the liquid's nonideality, and carries no pure-liquid reference fugacity at all). Nothing about it changed; the CLI is untouched.
- `tests/test_flash_modified_raoult.py` (new, 23), `tests/test_stability_candidates.py` (new, 23), `tests/validation/test_modified_raoult_vs_thermo.py` (new, 9)
  - LLE agreement with `gamma-gamma` to **1.7e-15** (the reference offset provably cancels between two liquids); bubble/dew located by bisecting `flash_tp` *verdicts* and checked against the scalar equations to **4.7e-15** / **2.6e-14**; the VL split satisfies `y P = x gamma Psat` to **1.6e-15** with mass balance **0.0** and `delta_g_split_rt = -2.0e-05`; the 1-propanol/water azeotrope from an independent bubble-curve bisection at **x1 = 0.419874, 360.918 K (87.77 C)** against a commonly tabulated (unverified) ~87.7 C / x1 ~ 0.43; water/1-butanol **T3 = 366.213774 K** with shared vapor **(0.234063, 0.765937)**, reproducing the independent reference to 1e-5; `thermo` 0.6.0 agreeing on Psat to **2.3e-15**, on bubble/dew to **2.9e-08 K** and on a VL flash to **|d beta| = 4.3e-06**.
  - **A useful identity fell out:** the verdict boundary sits exactly at the stability tolerance, `sum_i x_i gamma_i Psat_i / P = exp(tpd_tol)`, because the vapor trial's first substitution gives `W_i = x_i gamma_i Psat_i / P` and `tpd = -ln(sum W)`. Boundary tests therefore pass `StabilitySettings(tpd_tol=1e-14)`; this is asserted, not assumed.
  - **Two findings, recorded not accommodated.** (1) Just below T3 there is a window ~**0.135 K** wide where the deepest tangent-plane minimum is the vapor, the converged vapor-liquid pair is not the equilibrium, and `flash_tp` raises "a third phase is required". Refusing is right; the *diagnosis* is half right, because below T3 the resolution is a different pair of two, reached by adding the second liquid and removing the vapor. No retry-from-the-second-minimum heuristic was added - that is the next slice. (2) `thermo`'s VF-specified flash on this phase pair returns T = 1.8e5 K for a bubble point at 361 K; its T,P flashes are fine, so bubble/dew from `thermo` are bisected, and the failure is asserted so a future fix is noticed.
- `examples/basic/flash_tp_modified_raoult_demo.py` (new), `examples/validation/10_modified_raoult_water_butanol.py` (new), `README.md`, `examples/README.md`, `.agents/brain/adr/0010-stability-phase-candidates-modified-raoult.md` (new), `.agents/brain/validation-cases.md`, `.agents/brain/brain.md`
  - Golden paths for the three regimes from one call and for the whole three-phase neighbourhood with PASS/FAIL; README sections "Low-pressure vapor-liquid and liquid-liquid (`modified-raoult`)", "`gamma-phi` is deprecated" and "Adding an ideal-gas candidate (`vapor="ideal"`)", each stating the limits (ideal vapor, no Poynting, no `phi^sat`, enforced Antoine ranges, the refusal window near T3); validation Cases R-1 (LLE consistency), R-2 (VLE invariants + azeotrope), R-3 (three-phase controls + T3), R-4 (`thermo`).

- `src/chemthermo/flash/tp.py`, `src/chemthermo/flash/_detect.py` (new), `src/chemthermo/flash/_split.py` (new), `src/chemthermo/flash/_second_order.py` (new), `src/chemthermo/flash/_verify.py` (new), `src/chemthermo/flash/_assemble.py` (new), `src/chemthermo/flash/_legacy.py` (new) (slice `flash-module-split`)
  - Pure move refactor: `tp.py` (1572 lines) is now a thin orchestrator (297 lines, mostly its unchanged docstrings) that validates inputs, resolves the mode and dispatches to the internal modules above - phase detection/seeding, the shared K-loop, the liquid-liquid Newton stage, verification/post-split stability, `FlashResult` assembly, and the legacy `wilson-heuristic` path, one module per concern (mirrors the `stability/_evaluator.py` pattern). No algorithm was rewritten, no numpy operation order changed, no public name moved or renamed; `flash_tp`'s signature and docstring are unchanged. No ADR: internal structure only (ADR-0001, "anything outside [the public API sources] is internal"). Proven bit-identical by `tests/test_flash_refactor_bit_identity.py` against a fixture captured at the pre-refactor commit (155 states: the phi-phi grid, gamma-gamma binary/Tessier feeds, gamma-phi cases, both legacy cases), plus all 223 pre-existing tests unchanged.
- `src/chemthermo/flash/tp.py`, `src/chemthermo/flash/settings.py` (slice `flash-lle-activity`, ADR-0009)
  - `flash_tp` accepts `activity_model=` with **no** `eos`. `eos` gained a default of `None` and `flash_mode` a default of `None` = *infer from the models supplied*; an activity model alone resolves to the new `"gamma-gamma"` (liquid-liquid) mode. Naming a mode explicitly still wins, so `flash_mode="phi-phi"` without an `eos` and `"gamma-gamma"` with one are both `ModelError`. Phase count stays an output: the feed goes through `stability_tp(..., activity_model=...)` and a split is attempted only when it is found unstable. Phases are `"liquid1"` (feed-like) / `"liquid2"` (incipient-like) - roles, not identities, and they demonstrably swap between two feeds on one tie-line - with `vapor_fraction=None` and `equilibrium_residual` in place of `fugacity_residual`.
  - **Second-order stage**: a damped Newton *minimization* of the two-phase reduced Gibbs energy in the phase-II mole numbers, whose gradient is (Gibbs-Duhem) exactly the equal-activity residual; FD Hessian, eigenvalue shift, Armijo/residual backtracking inside `0 < n_i < z_i`. **Design deviation, recorded:** the brief's first option, Newton on the residual system in `(ln K, beta)`, was implemented and rejected on evidence - it walks into the trivial branch (`beta -> -8`) on the near-plait feed `z = (0.12, 0.05, 0.83)` and stalls at residual 1.4e-07 unless given 200+ substitutions first. A residual system cannot distinguish the equilibrium from the trivial solution; a descent method on `g` can. The stage is wired to the liquid-liquid split **only**, so no phi-phi number moved.
  - **Post-split stability**: every two-phase result on the tangent-plane phi-phi path and the gamma-gamma path re-tests each converged phase with `stability_tp`. A minimizer that *is* the partner phase (in the stability module's own `trivial_tol` metric) is `"marginal"`, not an instability - two coexisting phases share one tangent plane. Anything else negative raises `ConvergenceError` ("a third phase is required"); `FlashSettings(post_split_stability=False)` gates the *raise*, not the computation. `gamma-phi` and the legacy `wilson-heuristic` path cannot run it and report `post_split_checked=False` with a reason.
  - New `FlashSettings` fields: `post_split_stability`, `second_order`, `ssi_iterations` (50), `second_order_max_iter` (100), `second_order_tol` (1e-12).
- `tests/test_flash_lle.py` (new), `tests/validation/test_flash_lle_tessier2000.py` (new), `tests/test_flash_phase_detection.py`, `tests/test_cli_tp_flash.py`, `tests/fixtures/cli/*`
  - Mode contract, verdicts, binary binodal (0.019998419467 / 0.359999661508) and lever rule to 2.8e-12, determinism and permutation invariance of the phase *set*, `FlashResult` invariants, proof that SSI alone cannot solve the near-plait feed in the default budget, failure semantics; Tessier Problem 1 and 2 tie-lines against an independent SSI + damped-Newton solve of the full `(x^I, x^II, beta)` system (agreement <= 1.1e-12); phi-phi post-split grid (47/47 pass, worst post-split `tpd_min` -7.0055e-09); CLI fixtures gained the new diagnostics keys with `cli_schema_version` still 1.
  - **Two findings, recorded not accommodated:** (1) the binary feed x(n-butanol) = 0.30 is *inside* the miscibility gap and splits - the brief listed it as a single-phase control; 0.45 is used instead and `thermo` independently agrees 0.30 is unstable. (2) `thermo` 0.6.0's `FlashVLN` will not return a liquid-liquid split for two `GibbsExcessLiquid` phases over one NRTL model (`unique_liquid_count == 1`, single phase returned for every in-gap feed), so no flash-level comparison is possible; its own `stability_test_Michelsen` does find the conjugate pair and agrees to 1.0e-06 / 2.5e-05.
- `examples/basic/flash_tp_nrtl_lle_demo.py` (new), `examples/validation/09_lle_tessier2000_tie_lines.py` (new), `README.md`, `examples/README.md`, `.agents/brain/adr/0009-flash-liquid-liquid-activity.md` (new), `.agents/brain/validation-cases.md`, `.agents/brain/brain.md`
  - Golden paths for the binary tie-line/lever rule and for both published systems; README sections "Liquid-liquid flash (`gamma-gamma`)" and "Post-split stability" with the limits stated (two phases max, roles not identities, the check is only as sharp as the split, gamma-phi and legacy not checked); validation Cases L-1 (Problem 1 tie-lines), L-2 (Problem 2 + stable control + post-split), L-3 (binary binodal, lever rule, `thermo`), L-4 (phi-phi post-split grid).
- `src/chemthermo/flash/tp.py`, `src/chemthermo/flash/settings.py`
  - Phi-phi `flash_tp` now decides 1-vs-2 phases from `stability_tp` (ADR-0008): stable feed -> single phase (`termination_reason = "feed_stable_tangent_plane"`, name from the min-Gibbs `feed_branch`), unstable feed -> split seeded from the tangent-plane minimizer with the *unnormalized* mole numbers `W = w exp(-tpd)` (using normalized `w` makes `f_RR(0) = 0` exactly, an unbracketable root), inconclusive -> `ConvergenceError`. Wilson stays as a documented K-seed fallback (`diagnostics["k_seed"]`). Every converged split reports `mass_balance_residual`, `fugacity_residual` and `delta_g_split_rt`. New `FlashSettings.phase_detection` (`"tangent-plane"` default / `"wilson-heuristic"`) and `FlashSettings.stability_settings`. The split loop itself is unchanged and shared by both paths; gamma-phi stays on the heuristic and says so in diagnostics.
  - **Design deviation, recorded:** the brief specified orienting the seed from the stability result's `phase_branch`. That was implemented and rejected on evidence - when the cubic has a single real root both branch calls return identical `phi` and the label is only a tie-break, which returns the mirror-labelled solution (`beta = 0.3255` instead of the pinned `0.67451818` at Methane/Ethane 240 K / 3 MPa). The vapor/liquid *name* is now decided by the Wilson volatility *ranking* only (`ln(w_hi/z_hi) - ln(w_lo/z_lo) >= 0`); it never touches the verdict, the compositions or `beta`. See ADR-0008 decision 3.
- `src/chemthermo/cli.py`, `tests/fixtures/cli/tp_flash_v1.json`, `tests/fixtures/cli/tp_flash_gamma_phi_v1.json`, `tests/test_cli_tp_flash.py`
  - `solver.algorithm` now reports the path taken (`tangent-plane+...` vs `wilson+...`); `cli_schema_version` stays 1 (new keys live inside the free-form `diagnostics` mapping). Both fixtures' `diagnostics` blocks regenerated; the phi-phi fixture's `result` block was deliberately left at its pre-slice values and the contract test's tolerance relaxed from rel 1e-9 to 1e-7 (achieved 1.11e-9), so the fixture still pins the *old* equilibrium.
- `tests/test_flash_phase_detection.py` (new), `tests/validation/test_flash_phase_detection_vs_thermo.py` (new)
  - Regression (both canonical splits, bit-identical legacy numbers), verdict semantics, the disagreement state, a 144-state invariant grid (47 two-phase: mass balance <= 2.01e-13, fugacity residual <= 8.25e-9, `delta_g_split_rt <= -1.458e-4`), permutation invariance and determinism, failure semantics, and a 175-state `thermo` `FlashVL` cross-check (175/175 verdicts vs 166/175 for the heuristic).
- `examples/basic/flash_tp_auto_phase_demo.py` (new), `examples/basic/flash_tp_peng_robinson_demo.py`, `README.md`, `examples/README.md`
  - New golden path showing a stable feed, an unstable feed and the heuristic-vs-tangent-plane disagreement; the existing demo prints the new diagnostics keys; README gained an "Automatic phase detection" section that is explicit about the limits (two phases max, no post-split stability test, gamma-phi still heuristic, vapor/liquid naming is a convention).
- `.agents/brain/adr/0008-flash-tangent-plane-phase-detection.md` (new), `.agents/brain/validation-cases.md`, `.agents/brain/brain.md`
  - Recorded the decision, the rejected `phase_branch` orientation, the `cli_schema_version` reasoning, and validation Cases F-1 (thermo grid + regression), F-2 (the disagreement state) and F-3 (invariants).
- `src/chemthermo/stability/_evaluator.py` (new), `src/chemthermo/stability/tp.py`, `settings.py`, `results.py`
  - `stability_tp` now takes `eos=None, activity_model=None` (exactly one required; both raises `ModelError` because combined gamma-phi stability is out of scope, ADR-0007). Introduced the INTERNAL `_TangentPlaneEvaluator` protocol (`ln_fugacity_terms(w) -> (ndarray, str | None)`, `initial_estimates(z, active)`) with `_EOSTangentPlane` (ln phi on the min-Gibbs root, Wilson + pure trials) and `_ActivityTangentPlane` (ln gamma, pure-component trials only); the solver, trivial detection, summary and result types no longer know the model family. Added a damped-Newton second stage in `ln W` (FD Jacobian, step cap, backtracking line search) after `settings.ssi_iterations`; new settings `second_order`, `ssi_iterations` (50), `second_order_max_iter`, `second_order_max_step`; new trial fields `ssi_iterations`, `second_order_iterations`, `converged_stage`; `feed_branch`/`phase_branch` are `None` for activity models. Peng-Robinson results are bit-identical (pinned to 1e-12 with per-trial iteration counts).
- `tests/fixtures/nrtl/tessier2000_problem2.json` (new), `tests/conftest.py`
  - Cited Tessier (2000) Table 4 parameters for n-propanol / n-butanol / benzene / water. Table 4 prints G and tau, not alpha; recovered alpha is symmetric to 2.597e-05 and the rounded symmetric average reproduces the printed G to 4.413e-06. Third-party DECHEMA-regressed data: test fixture only, never packaged runtime data.
- `tests/test_stability_activity.py`, `tests/validation/test_stability_nrtl_tessier2000.py`, `tests/test_stability_tp.py`
  - Activity-path invariants (tangent-plane identity, stationarity + FD gradient of tm, tm/tpd/sum(W), permutation invariance, determinism, degenerate feeds), negative controls (pure component, ideal solution, n-butanol/water LLE with an independent binodal solve), a proof that SSI alone cannot solve the near-plait feed, the Tessier Problem 1 / Problem 2 reproduction, a `thermo` 0.6.0 ln-gamma cross-check (max |dD| = 4.7e-16), and a bit-level PR regression pin.
- `examples/basic/stability_tp_nrtl_lle_demo.py`, `examples/validation/08_stability_nrtl_tessier2000.py`, `README.md`, `examples/README.md`
  - Golden paths for the activity-model stability verdict and for the published-minima reproduction (neither needs `thermo`), plus README coverage of the activity usage and the gamma-phi "not supported" note.
- `.agents/brain/adr/0007-stability-tangent-plane-evaluator.md`, `.agents/brain/validation-cases.md`, `.agents/brain/brain.md`
  - Recorded the evaluator contract (and why it stays internal), and added validation Cases S-6 (Problem 1 minima), S-7 (Problem 2 minima + the stable control + one disputed printed D) and S-8 (n-butanol/water LLE control).
- `src/chemthermo/models/nrtl.py`
  - Corrected the NRTL activity-coefficient equation. The previous code computed `S = G @ x` (row sums) and divided the first term term-by-term; the standard Renon-Prausnitz form needs the column sums `S_j = sum_k G_kj x_k`, `C_j = sum_k tau_kj G_kj x_k` and a single denominator `S_i` in the first term. The implementation is now vectorized (`G.T @ x`, `(tau * G).T @ x`) and the module docstring derives the equation and states the index convention (`tau[i, j] = tau_ij`, `G_ij = exp(-alpha_ij tau_ij)`). Public signature and the single-component `[1.0]` shortcut unchanged; returned values change for asymmetric parameters (pre-fix: Gibbs-Duhem residuals ~1e-1, up to 0.89 off in `ln gamma` versus `thermo.NRTL`).
- `src/chemthermo/parameters/data/activity/nrtl.json`, `src/chemthermo/parameters/nrtl.py`
  - Labelled the two packaged pairs (Methane/Ethane, Benzene/Water) honestly: new top-level `provenance` block and per-pair `"source": "synthetic-demo"`. They are illustrative placeholders added ad hoc in commits e5ccd8f/ecd476e, not fitted or published data. The loader already ignored unknown keys, so no `schema_version` change; that tolerance is now documented in the `NRTLParameters` docstring together with the index convention.
- `tests/fixtures/nrtl/tessier2000_problem1.json`, `tests/conftest.py`
  - Cited published parameter set (Tessier, Brennecke & Stadtherr, Chem. Eng. Sci. 55 (2000) 1785, Table 1; parameters originally McDonald & Floudas, AIChE J. 41 (1995) 1798) plus Table 2 stationary points, deliberately kept out of the packaged defaults and loaded via `NRTLParameters.from_pairs`. New `tests/conftest.py` exposes it as session fixtures.
- `tests/test_activity_nrtl.py`, `tests/validation/test_nrtl_tessier2000.py`
  - Gibbs-Duhem (4 compositions x 4 simplex directions, worst residual 2.07e-10), binary reduction against independently written two-component formulas (1.1e-16), permutation invariance, symmetric-ternary and zero-tau limits, a hard-coded regression guard against the row-sum bug, a `thermo` 0.6.0 cross-check with the asymmetric Tessier parameters (max |d ln gamma| 8.88e-16), and the Table 2 reproduction with a from-scratch tangent-plane distance and damped-Newton stationary-point solve.
- `examples/validation/07_nrtl_tessier_stationary_points.py`, `examples/README.md`, `README.md`
  - Golden path reproducing Table 2 (no optional dependency), plus a README "NRTL activity coefficients" section carrying the equation, the correctness note and the synthetic-parameter disclosure.
- `tests/fixtures/cli/tp_flash_gamma_phi_v1.json`
  - Regenerated: the corrected gammas move the CLI gamma-phi Methane/Ethane result (vapor fraction 0.7670920475829917 -> 0.7648352545438684). Schema, phase names, iteration count and termination reason unchanged.
- `.agents/brain/validation-cases.md`, `.agents/brain/brain.md`
  - Added validation Cases N-1 (Gibbs-Duhem + binary reduction), N-2 (`thermo` cross-check) and N-3 (Tessier Table 2, including two printed D values documented as typographical errors rather than accommodated), and a new brain invariant that activity models must satisfy Gibbs-Duhem and must be tested with asymmetric parameters.
- `src/chemthermo/models/peng_robinson.py`
  - Fixed the diagonal-kij bug: `aij`'s diagonal is now always unaffected by `kij` (pure-component `a_ii` never corrupted). Added per-pair `kij` support (`float` or `Mapping[tuple[str, str], float]` keyed by normalized component names, canonicalized to a sorted tuple in `__post_init__`), plus two private helpers (`_kij_matrix`, `_mixture_parameters`) so `fugacity_coefficients`/`compressibility_factor` no longer duplicate the mixing-rule code. Public method signatures unchanged; `kij=0.0` (default) is bit-identical to before.
- `tests/test_pr_eos.py`, `tests/validation/test_pr_kij_vs_thermo.py`
  - Unit coverage (pure-component invariance, diagonal-not-corrupted, scalar-vs-mapping equivalence, name normalization/symmetry, conflicting/identical-pair `ModelError`, unknown-pair no-op, permutation invariance, flash regression unchanged) and a `thermo` 0.6.0 PRMIX cross-check (binary + synthetic 3-component kij matrix, pure-component limit, end-to-end `flash_tp`/`stability_tp` vs `FlashVL`/`stability_test_Michelsen`).
- `examples/basic/tp_flash_pr_kij_demo.py`, `examples/README.md`, `README.md`
  - Golden path contrasting `kij=0.0` against a per-pair mapping on the same feed; README "Binary interaction parameters (kij)" section.
- `.agents/brain/adr/0006-pr-kij-matrix.md`, `.agents/brain/validation-cases.md`, `.agents/brain/brain.md`
  - Recorded the per-pair kij API decision (ADR-0006) and added validation Case K-1 (pre-fix vs post-fix discrepancy against `thermo`: ~0.365 max |d ln phi| before, ~3.4e-4 after, at one representative state).
- `pyproject.toml`, `tests/test_packaging_constraints.py`, `README.md`
  - Constrained the `bibtexparser` dependency to `>=1.4.0,<2`: a fresh (non-editable) install previously resolved `bibtexparser` 2.x, whose removed `bparser`/`customization` modules `src/chemthermo/citations.py` imports at module load time, breaking `import chemthermo` entirely; added a test that reads the specifier from `pyproject.toml` and a README "Common issues" bullet.
- `src/chemthermo/stability/` (`__init__.py`, `tp.py`, `results.py`, `settings.py`), `src/chemthermo/__init__.py`
  - Added public `stability_tp` (Michelsen tangent-plane stability at fixed T, P, z) with `StabilityResult`, `StabilitySettings`, `StabilityTrial`; min-Gibbs compressibility root selected generically by minimizing `sum_i w_i ln phi_i(w)` over the existing `EquationOfState` interface.
- `tests/test_stability_tp.py`, `tests/validation/test_stability_vs_thermo.py`
  - Invariant tests (tangent-plane identity, stationarity plus finite-difference gradient of tm, tm/tpd/sum(W) relations, marginal stability of converged equilibrium phases, permutation invariance, degenerate feeds) and a 7-state cross-check against `thermo` 0.6.0.
- `examples/basic/stability_tp_peng_robinson_demo.py`, `examples/validation/06_stability_vs_thermo.py`, `examples/README.md`, `README.md`
  - Golden path for the stability verdict and a deterministic external validation script.
- `.agents/brain/adr/0005-stability-tp-public-api.md`, `.agents/brain/validation-cases.md`, `.agents/brain/brain.md`
  - Recorded the public stability API decision and opened the validation-case ledger ("thermodynamics exam").
- `src/chemthermo/cli.py`, `tests/test_cli_tp_flash.py`, `tests/fixtures/cli/tp_flash_gamma_phi_v1.json`
  - Added `--flash-mode {phi-phi,gamma-phi}` to `tp-flash`, wired gamma-phi to `NRTL()` activity model, and expanded deterministic CLI contract tests/fixture coverage.
- `.agents/brain/adr/0004-cli-tp-flash-gamma-phi.md`, `.agents/brain/brain.md`
  - Recorded CLI gamma-phi public-contract decision and advanced roadmap slices.
- `tools/build_database.py`
  - Rebuilt canonical DB generation to match runtime schema, added deterministic `--check` against `src/chemthermo/data/components.json`, and added optional non-runtime mirror output at `database/components.mirror.json`.
- `src/chemthermo/cli.py`, `src/chemthermo/__main__.py`, `pyproject.toml`
  - Added public `chemthermo` CLI entrypoint and module execution with `tp-flash` command.
- `tests/test_database_build_tool.py`, `tests/test_cli_tp_flash.py`, `tests/validation/test_reference_case.py`, `tests/fixtures/cli/tp_flash_v1.json`
  - Added focused tests for DB tooling, CLI contract/exit codes, and optional external validation reference case.
- `examples/database/01_component_database_demo.py`, `examples/validation/00_reference_case.py`
  - Added golden-path scripts for DB visibility and deterministic external validation.
- `README.md`, `database/README.md`, `examples/README.md`, `.gitignore`
  - Clarified canonical DB path policy and documented CLI/validation workflows.
- `.agents/brain/adr/0003-cli-entrypoint.md`, `.agents/brain/brain.md`
  - Recorded CLI API decision and updated architecture/public API status.

## Current architecture (8-12 lines)
- `chemthermo.eos.PCSAFTEOS` speaks **both** state specifications: `(T, molar density, x)` in for `A^res/RT` / `Z` / `P` / `ln phi_i` (ADR-0014), and `(T, P, x, phase)` in for `fugacity_coefficients` / `density_roots` / `molar_volume` (ADR-0015), the latter solving `P_model = P` for its mechanically stable roots in the private `eos/_pcsaft_density.py` (the model restricted to one `(T, x)` is a function of the packing fraction alone, which makes the scan vectorizable and `dP/drho` analytic). Because it now satisfies `chemthermo.models.EquationOfState`, it joins the flash and the stability test **through the same interface Peng-Robinson uses and with no solver change**: `_EOSTangentPlane`'s existing lowest-Gibbs selection over the `"vapor"` / `"liquid"` branches is already the right rule for density roots. It shares the `kij` contract with Peng-Robinson through the internal `models/_kij.py`.
- `NRTL` implements the standard Renon-Prausnitz equation (column sums); it satisfies Gibbs-Duhem to ~2e-10 and matches `thermo` to ~9e-16 with asymmetric parameters. Packaged pair parameters are synthetic placeholders; published sets live in `tests/fixtures/`.
- `PengRobinsonEOS.kij` is a scalar (off-diagonal only) or a name-keyed per-pair `Mapping`; `flash_tp` and `stability_tp` results for nonzero kij are now trustworthy (ADR-0006).
- Phase stability (`chemthermo.stability`) is a solver-independent sibling of `chemthermo.flash`, and `flash_tp` consumes it twice: `flash_tp -> stability_tp -> split -> post-split stability` (ADR-0008, ADR-0009, ADR-0010), for phi-phi (EOS), gamma-gamma (activity, liquid-liquid) and modified-raoult (activity liquid + ideal vapor, VLE *or* LLE) alike. It serves all three through the internal `_TangentPlaneEvaluator` contract, which now holds a *set* of `_PhaseCandidate`s and keeps the lowest-Gibbs one at each composition (min-Gibbs cubic root; single liquid; liquid-versus-vapor). Each trial runs successive substitution then an optional damped-Newton stage.
- `Psat_i(T)` from the databank Antoine records lives in `src/chemthermo/models/_antoine.py` (internal), in the stored form `ln(P^sat/bar) = A - B/(T+C)`; it is the modified-Raoult reference fugacity and is refused outside each fit's stated range.
- Canonical runtime DB path is `src/chemthermo/data/components.json`.
- Runtime DB loading is package-resource based via `chemthermo.data` helpers.
- Raw DB source tables stay in `database/organics.txt` and `database/inorganics.txt`.
- `tools/build_database.py` is the canonical regeneration/check tool for packaged DB sync.
- Optional mirror output path is `database/components.mirror.json` (non-runtime, generated-only).
- Core thermodynamic flow is `Component/Composition/Mixture -> models -> flash_tp -> stability_tp -> seeded split (+ second-order stage for LLE) -> post-split stability -> FlashResult` (phi-phi and gamma-gamma); gamma-phi still goes straight to the Wilson-seeded split and is not post-split checked.
- CLI `tp-flash` maps inputs to `Mixture + PengRobinsonEOS + flash_tp`, with optional `NRTL()` activity model when `--flash-mode gamma-phi`.
- CLI supports deterministic text/json outputs with schema version and diagnostics.
- External validation remains optional and script-driven (`examples/validation/00_reference_case.py`).

## Public API status (stable vs experimental)
- Stable:
  - `chemthermo` Python exports in `src/chemthermo/__init__.py`, including `stability_tp` / `StabilityResult` / `StabilitySettings` / `StabilityTrial` (ADR-0005, ADR-0007). `stability_tp` takes exactly one of `eos=` / `activity_model=`.
  - `PengRobinsonEOS(kij=...)` scalar-or-mapping constructor contract (ADR-0006).
  - `FlashSettings(phase_detection=..., stability_settings=...)` and the `flash_tp` diagnostics keys listed in its docstring (ADR-0008).
  - `flash_tp(..., eos=None, activity_model=..., flash_mode=None)` liquid-liquid mode, the `liquid1`/`liquid2` role naming, `vapor_fraction=None` for gamma-gamma, the post-split diagnostics keys, and `FlashSettings(post_split_stability=..., second_order=..., ssi_iterations=..., second_order_max_iter=..., second_order_tol=...)` (ADR-0009).
  - `flash_tp(..., flash_mode="modified-raoult")` and `stability_tp(..., activity_model=..., vapor="ideal")`, the `"liquid"`/`"vapor"` candidate labels in `feed_branch`/`phase_branch`, `model_family == "modified-raoult"`, and the `antoine_valid_Tmin_K` / `antoine_valid_Tmax_K` diagnostics (ADR-0010).
  - `FlashSettings(max_phases=3)` and the phase addition/removal search on the `modified-raoult` path (ADR-0011); `StabilityTrial.surface` and the fixed-candidate-surface trial set (ADR-0012).
  - `chemthermo.eos` registry (`EOSProtocol`, `get_eos`, `list_eos`) and `PCSAFTEOS` (ADR-0014, ADR-0015): `PCSAFTEOS(components=(), parameters=None, kij=0.0)` with `residual_helmholtz(temperature_K=, volume_m3=, composition=)` (molar volume), the density-based `compressibility_factor` / `pressure_Pa` / `ln_fugacity_coefficients`, and the pressure-based `fugacity_coefficients(mixture=, temperature_K=, pressure_Pa=, composition=, phase=)` / `density_roots(...)` / `molar_volume(..., phase=)`. `PCSAFTEOS` is now in `chemthermo.__all__` and is an `EquationOfState`. Top-level `PCSAFTParameters`, `PCSAFTRecord`, `PCSAFTParameterError` and `chemthermo.parameters.get_pcsaft_parameters`.
- Deprecated (unchanged, not removed):
  - `flash_mode="gamma-phi"`. Double-counts liquid nonideality and has no pure-liquid reference fugacity; use `"modified-raoult"` at low pressure and `"phi-phi"` at high pressure. Removal would need its own ADR.
  - `chemthermo.vlle` documented public subpackage (`get_vlle_engine`, `VLLEEngine`, `VLLEResult`) - now itself **deprecated** (ADR-0013, see the top of this brief): importing it or calling `get_vlle_engine()` emits `DeprecationWarning`; names stay importable for one deprecation cycle; removal needs its own ADR.
  - CLI script `chemthermo` with `tp-flash` subcommand and defined exit-code contract.
- Experimental/placeholder:
  - Nothing in `chemthermo.eos` is a placeholder any more. `PCSAFTEOS` implements non-associating PC-SAFT, is wired into `stability_tp` / `flash_tp`, and is validated against teqp at the property level and at the equilibrium level (ADR-0014, ADR-0015); its limits are stated in README's "Limits, stated plainly" and are gaps, not scaffolding.
  - Validation policy promotion to required CI remains deferred.

## Risks / unknowns
- **PC-SAFT is honest only for non-associating, non-polar fluids, and nothing in the code enforces that.** The packaged parameter file contains only such compounds, but `PCSAFTParameters.from_records(...)` will happily accept water-like parameters and the model will return confident, wrong numbers. The association term (Gross & Sadowski, IECR 41 (2002) 5510) and the polar terms are not implemented; the README says so, the physics does not.
- **PC-SAFT's density root scan can miss a pair of roots that is narrower than its grid step** (`5e-4` in packing fraction), and the state is then reported with one root fewer. That happens only where the isotherm is nearly tangent to the target pressure - a near-critical or near-spinodal state - where the missed phase is physically marginal. The step is a named constant so the trade is explicit, not hidden (ADR-0015, Case P-3).
- **The `(T, rho, x)` methods still choose nothing**, and that is deliberate: between the spinodals `Z < 0` and `ln phi` does not exist, and a caller who picks a density on the wrong branch gets a valid-looking answer for a state they did not mean. Use `density_roots` or the `phase=` interface when the model should choose. Root *selection* stays with the phase-candidate machinery (`_select_min_gibbs`), not buried in the equation of state.
- **The residual `|P_model - P|/P` at a returned root is limited by cancellation, not by the iteration.** On a dense liquid branch at a low pressure `Z` is a near-total cancellation (1.17e-3 at n-hexane's 300 K saturation state), so a 1e-14 relative error in `a'` is 1e-11 in `P`. Measured 2.2e-14 at 10 MPa and 6.5e-12 at 21.9 kPa. The *density* is unaffected (2.2e-16 against teqp) and is what the tests pin; `DensityRoots.max_relative_residual` reports the number rather than hiding it.
- **`flash_tp` with PC-SAFT still stops at two phases**, like every phi-phi state (ADR-0009, ADR-0011), and no phase densities are carried in `FlashResult` - they have to be recomputed with `density_roots` at the converged composition.
- **PC-SAFT's parameter provenance is second hand.** The primary paper could not be opened (ACS 403). Two independent secondary sources agree digit for digit on both tables, and teqp reproduces every number to 1e-14 from the same values, so a transcription error would have to be shared by teqp, FeOs, Clapeyron.jl and Wikipedia - but "verified against the paper" is *not* a claim this repository can make today.
- **No PC-SAFT temperature derivative**, so no residual enthalpy/entropy and no cross-check against teqp's `get_Ar10`. That column of the comparison is empty and is recorded as empty in Case P-1.
- **Near a three-phase state the modified-Raoult path refuses over a finite window, and its message is only half right.** Measured for water/1-butanol at 1 atm, z(butanol) = 0.20: from about T3 - 0.135 K up to T3 = 366.2138 K, the deepest tangent-plane minimum is the *vapor*, the split converges a vapor-liquid pair whose liquid lies inside the miscibility gap, and the post-split test raises "a third phase is required". Refusing is correct (that pair is not the equilibrium), but below T3 the resolution is a *different pair of two*, reached by adding the second liquid and then removing the vapor. `flash-vlle-phase-addition` must handle removal, not only addition. Validation Case R-3, ADR-0010 "Known limitation, measured".
- The modified-Raoult model is honest only at low pressure: ideal vapor (no `phi^V`), no Poynting correction, no `phi^sat`, and a temperature range bounded by the Antoine fits (enforced with `InputRangeError`, never extrapolated). Nothing in the code stops a user from calling it at 50 bar; the docs say not to, and the physics does not.
- `flash_tp` still returns at most two phases. A state needing a third is now *detected* by the post-split check and refused with `ConvergenceError`, not solved. Both the feed verdict and the post-split verdict inherit `stability_tp`'s bound: "stable" means nothing negative was found from the deterministic trial set, so a third phase no trial reaches would still be missed.
- The post-split check is only as sharp as the split it checks. Measured margin at default settings is large (worst post-split `tpd_min` -7.0e-09 against `tpd_tol` 1e-8 - a factor of 1.4 - with the minimizer matching the partner phase to `sum ln(w/x)^2 <= 4e-16`, twelve orders inside `trivial_tol`), but with a deliberately loosened `FlashSettings(tol=1e-3)` a genuinely two-phase state is reported as needing a third phase. Documented in README and ADR-0009.
- Every two-phase flash on the tangent-plane paths now pays for two extra stability analyses (measured: 0.9 s for the 47 two-phase states of the in-repo grid, roughly doubling their cost). There is no way to skip the computation, only the raise.
- `liquid1`/`liquid2` carry no physical identity and can swap between feeds on the same tie-line. Any consumer that indexes `result.phases["liquid1"]` expecting a particular liquid is wrong.
- The vapor/liquid *name* of a `flash_tp` phase is a convention in two places: a single-root (dense/supercritical) single-phase feed gets whichever branch label wins the min-Gibbs tie-break, and a two-phase result is named by Wilson volatility ranking. `EquationOfState` exposes no molar volume, so nothing better is available without widening that protocol.
- Every phi-phi flash now pays for a stability analysis (2 Wilson trials + 1 pure trial per component) even for an obviously single-phase feed.
- 5 states in the 1144-state scan (Methane/Propane/n-Decane 0.7/0.2/0.1 at 1.2e7-2.0e7 Pa) split in chemthermo (`delta_g_split_rt < 0`) while `thermo`'s `FlashVL` returns `VF = 0`. They disagreed with `thermo` before this slice too, so they are not caused by it. Now adjudicated: evaluating `thermo`'s own `lnphis_at_zs(..., most_stable=True)` at chemthermo's `stability_tp` minimizer gives a negative tpd (within 5e-5 abs of chemthermo's `tpd_min`) at all 5 states, so `thermo`'s `VF = 0` is a stability-*search* miss on `thermo`'s side, not a chemthermo error. See Case F-1 in `validation-cases.md` and `tests/validation/test_flash_thermo_disagreements_adjudicated.py`.
- `stability_tp` reporting "stable" is bounded by its deterministic trial set; it is not a global proof, and the docs must keep saying so. Measured limit: on Tessier (2000) Problem 2 the trial set reaches 7 of the 10 non-trivial published stationary points; the three it misses all have D > 0 (Cases S-6, S-7).
- Combined gamma-phi stability (activity liquid vs EOS vapor) raises `ModelError`. It stays unsupported until there is a consistent pure-liquid reference fugacity (ADR-0007); the current gamma-phi flash does not carry one correctly.
- The second-order stage uses a central-difference Jacobian of `ln phi`/`ln gamma`. For a cubic EOS the min-Gibbs branch can switch between finite-difference probes, which would make that Jacobian noisy; it has not been observed because every validated PR state converges inside the 50-iteration SSI budget and never enters the stage. Watch for it if PR states that need the stage ever appear.
- chemthermo uses the rounded PR constants 0.45724 / 0.07780 while `thermo` uses the exact roots; this bounds external agreement at a few times 1e-4 in ln(phi) (was ~2e-4 at the stability states, ~4-6e-4 at some nonzero-kij states -- see Case K-1).
- The illustrative kij value used in docs/examples/tests (0.0411, Methane/n-Decane) is explicitly NOT a validated literature parameter -- do not let it drift into being read as one.
- `thermo`'s own `CEOSLiquid`/`CEOSGas` root solver was observed to be numerically order-sensitive (not permutation-invariant) at some near-critical-locus states during `pr-kij-matrix` development; avoid cross-checking permutation invariance against `thermo` at such states (use chemthermo-internal invariance checks instead, as `tests/validation/test_pr_kij_vs_thermo.py` now does).
- CLI JSON contract must remain backward-compatible or version-bumped.
- DB builder currently assumes raw table column conventions stay unchanged.
- Optional validation still depends on external `thermo` package availability.
- (Removed) Unpinned `bibtexparser>=1.4.0` allowed a fresh/non-editable install to resolve `bibtexparser` 2.x, whose removed `bparser`/`customization` modules broke `import chemthermo`; this was invisible locally because the dev venv already had 1.4.4 installed. Now pinned to `>=1.4.0,<2` and covered by `tests/test_packaging_constraints.py`.

## Next 3 recommended actions
- **`pcsaft-association` (recommended next).** It is the only remaining gap that makes a *named class of fluids* unusable rather than making an existing answer nicer: water, alcohols, acids and amines are off limits without the association term (Gross & Sadowski, IECR 41 (2002) 5510), and those are exactly the systems the `modified-raoult` path already covers with NRTL - so the package can model them at low pressure with an activity model and not at all with an equation of state. It also has the same validation route as everything else in the family (teqp implements the associating model too), and it is what would supply a genuine three-phase EOS state, which the `flash-phase-addition-eos` item below is currently blocked on. Shape: association metadata in the packaged parameter file, the term and its two derivatives, the inner site-fraction solve (which has to converge inside the density scan), and a flash validation against teqp for a water or alcohol binary. Nothing outside `chemthermo.eos` should need to change, exactly as in ADR-0015.
- `flash-phase-addition-eos`: wiring the phase addition/removal search (ADR-0011) to **phi-phi** and **gamma-gamma**. Considered as the next slice and **not** recommended yet: the machinery is generic and it is a few lines each, but no state in this repository exercises a third phase on either path (Case L-4), and PC-SAFT joining the EOS family did not supply one - a non-associating hydrocarbon binary has no third phase. It would be a scaffolded feature under ADR-0002 until a trigger state exists, and the likeliest source of one is an associating system.
- Wire the existing second-order stage into the **phi-phi** split: the one state in 1144 that still raises `ConvergenceError` is weakly unstable and near-critical (`tpd_min = -1.2e-3`). The machinery exists; it was deliberately kept off that path so no validated phi-phi number moved in `flash-lle-activity`.

## One simplification / deletion candidate
- Remove or archive deprecated `database/components.json` once all docs/tooling users are migrated.

## Assumptions I'm making
- Runtime DB source of truth remains `src/chemthermo/data/components.json`.
- `database/components.mirror.json` is generated-only and should not be committed.
- CLI v1 scope remains TP flash with Peng-Robinson EOS and `--flash-mode {phi-phi,gamma-phi}`.

## How to validate quickly
- `python examples/basic/flash_tp_peng_robinson_demo.py`
- `python examples/basic/pcsaft_properties_demo.py`
- `python examples/validation/13_pcsaft_vs_teqp.py` (with `pip install -e ".[validation]"`)
- `python examples/basic/flash_tp_pcsaft_demo.py`
- `python examples/validation/14_pcsaft_flash_vs_teqp.py` (with `pip install -e ".[validation]"`)
- `python examples/basic/flash_tp_auto_phase_demo.py`
- `python examples/basic/stability_tp_nrtl_lle_demo.py`
- `python examples/basic/flash_tp_nrtl_lle_demo.py`
- `python examples/basic/flash_tp_modified_raoult_demo.py`
- `python examples/validation/10_modified_raoult_water_butanol.py`
- `python examples/validation/08_stability_nrtl_tessier2000.py`
- `python examples/validation/09_lle_tessier2000_tie_lines.py`
- `python examples/basic/tp_flash_pr_kij_demo.py`
- `python -m pytest tests/validation/test_pr_kij_vs_thermo.py -q` (with `pip install -e ".[validation]"`)
- `python examples/basic/stability_tp_peng_robinson_demo.py`
- `python examples/validation/06_stability_vs_thermo.py` (with `pip install -e ".[validation]"`)
- `python tools/build_database.py --check`
- `python examples/database/01_component_database_demo.py`
- `python -m chemthermo tp-flash --components Methane,Ethane,Propane --z 0.5,0.3,0.2 --temperature-k 240 --pressure-pa 3000000 --format json`
- `python -m chemthermo tp-flash --components Methane,Ethane --z 0.5,0.5 --temperature-k 240 --pressure-pa 3000000 --flash-mode gamma-phi --format json`
- `python examples/validation/00_reference_case.py` (with `pip install -e ".[validation]"`)
