# Archive

Legacy prototypes, notebooks, and early experiments are stored here to preserve
history while the `chemthermo` package is rebuilt under `src/`.

- `legacy-scripts/`: standalone scripts and older EOS experiments.
- `legacy-flash/`: prior flash package implementation.
- `legacy-notebooks/`: exploratory notebooks.
